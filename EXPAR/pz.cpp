#include <iostream>
#include <cstring>
using namespace std;






int main(){



    return 0;
}

//strcat






























/*
#include <iostream>
using namespace std;

int buscarLineal(int arr[], int n, int objetivo) {
    // Recorremos el arreglo desde el índice 0 hasta n-1
    for (int i = 0; i < n; i++) {
        // Si el elemento actual es igual al que buscamos
        if (arr[i] == objetivo) {
            return i; // Éxito: retornamos la posición
        }
    }
    return -1; // Fallo: recorrimos todo y no estaba
}

int main() {
    int datos[] = {14, 2, 8, 33, 5}; // No importa el orden
    int n = 5;
    int buscar = 33;

    int indice = buscarLineal(datos, n, buscar);

    if (indice != -1)
        cout << "Lineal: Encontrado en indice " << indice << endl;
    else
        cout << "Lineal: No encontrado" << endl;

    return 0;
}
*/